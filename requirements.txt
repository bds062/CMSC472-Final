# Minimal dependencies for the corrected EEGNet / SEED-IV codebase.
# Standard-library imports such as argparse, json, os, pathlib, random,
# itertools, time, traceback, dataclasses, and typing are intentionally omitted.

# Numerical/scientific computing
numpy>=1.26,<3
scipy>=1.11,<2
h5py>=3.10,<4

# Machine learning / evaluation
scikit-learn>=1.4,<2

# Deep learning
# For CUDA-specific installs, use the PyTorch selector to choose the correct wheel.
torch>=2.4,<3

# Plotting and sweep analysis
matplotlib>=3.8,<4
pandas>=2.1,<3

# yaml
pyyaml
